name: cge-test
channels:
- conda-forge
dependencies:
  # Base dependencies
  - pip
  - numpy
  - scipy
  - sympy
  - pandas
  - numba
  - IPython
  - pytensor
  - tqdm
  - numba-progress
  - fastprogress

  - pip:
    - latextable
    - texttable
    - sympytensor

  # Testing dependencies
  - pre-commit
  - pytest-cov>=2.5
  - pytest>=3.0
  - pytest-env
